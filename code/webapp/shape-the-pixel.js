import { ViewState } from './state/view-state.js'

window.onload = () => {
  // const viewState = new ViewState(window.innerWidth, window.innerHeight)
  // const view = new View()

  // window.location.hash = viewState.actorId
  // view.initialize(viewState)
  console.log('READY')
}
